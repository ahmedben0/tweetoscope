// the class “Processor” handles the tweets for a given source
// what ? : when getting a tweet, we have to identify the source
// theeen redirect the processing of the tweet to the unique processor that handles that source

// in the main, create different instances of the class Processor
  // if Processor for a given tweet id is created then just do the processing with this new tweet as well
  // if not, create a new object Processor
  
