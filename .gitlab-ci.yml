stages:
    - build_docker
    - build
    - test


build_docker:
    image: docker:latest
    stage: build_docker
    services:
        - docker:dind
    before_script:
        - docker login -u "$Dockerhub_username" -p "$Dockerhub_password"
    script:
        - apk update && apk add git
        - docker build -f docker/Dockerfile.main -t $Dockerhub_username/$Docker_image_name:latest .
        - docker push $Dockerhub_username/$Docker_image_name:latest
    rules:
        - changes:
            - docker/Dockerfile.main


build:
    image: $Dockerhub_username/$Docker_image_name:latest
    stage: build
    tags:
        - docker
    script:
        - mkdir build
        - cd build
        - cmake ..
        - make


test:
    image: $Dockerhub_username/$Docker_image_name:latest
    stage: test
    tags:
        - docker
    script:
        - echo "add tests"
