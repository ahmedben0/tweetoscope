stages:
    - build
    - test
        

build:
    stage: build
    script:
        - docker build -f Dockerfile.cppfile -t cpp-docker-image .
        - echo "Building"
        - echo "to be done"


test:
    stage: test
    script:
        - echo "Testing"
        - echo "to be done"
