stages:
    - build
    - test


build:
    stage: build
    image: fellajimed/wikipedia-docker-agg:latest
    script:
        - echo "Building"
        - echo "to be done"


test:
    stage: test
    script:
        - echo "Testing"
        - echo "to be done"
