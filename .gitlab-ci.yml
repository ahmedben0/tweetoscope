stages:
    - build
    - test

image:
    stage: build
    script:
        - docker build -f Dockerfile.cppfile -t cpp-docker-image .

build:
    stage: build
    script:
        - echo "Building"
        - echo "to be done"


test:
    stage: test
    script:
        - echo "Testing"
        - echo "to be done"
